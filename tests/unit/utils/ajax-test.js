import ajax from 'smart-cradle-web/utils/ajax';
import { module, test } from 'qunit';

module('Unit | Utility | ajax');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ajax();
  assert.ok(result);
});
